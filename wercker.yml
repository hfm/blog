box: wercker/rvm
build:
    steps:
        - rvm-use:
              version: 2.1.4
        - bundle-install
        - script:
            name: build site with jekyll
            code: |
                bundle exec jekyll build
